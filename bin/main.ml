open Batteries

open Jcat.Machine
open Instructions

(* let () = infer @@ expr [quote @@ expr [int 0]; call] *)
(* let () = infer @@ expr [int 0; quote @@ expr [int 1; plus]; call; plus] *)
(* let () = infer @@ expr [bool true; plus] *)
